# 직접 의존 — 코드가 import하는 것. 올릴 때는 여기를 고치고 아래 간접 의존도 함께 갱신한다.
#
# 전부 버전을 고정한다. 이미지를 빌드할 때마다 그 시점 최신이 설치되면 테스트한 조합과
# 배포되는 조합이 달라진다(openai·langchain은 메이저 변경이 잦다).
# 버전을 올린 뒤에는 linux/amd64 휠이 있는지 확인한다 — ACA는 amd64에서 돈다:
#   grep -v kiwipiepy-model requirements.txt > /tmp/req.txt
#   pip download --only-binary=:all: --no-deps --python-version 3.12 --implementation cp \
#       --platform manylinux_2_28_x86_64 --platform manylinux2014_x86_64 -r /tmp/req.txt -d /tmp/wheels
# kiwipiepy-model만 예외다. 휠 없이 소스(88MB, 형태소 모델 데이터)로만 배포되지만 컴파일할
# 것이 없어 py3-none-any로 빌드된다 — 컴파일러 없는 slim 이미지에서도 설치된다.
requests==2.34.2
psycopg2-binary==2.9.12
python-dotenv==1.2.3
langchain_openai==1.6.0
kiwipiepy==0.23.2
openai==3.8.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic-settings==2.15.0

# 간접 의존 — 위 패키지들이 끌고 오는 것. 직접 import하지 않지만 함께 고정해야
# 빌드가 재현된다(httpx·pydantic-core 같은 하위 패키지가 바뀌어도 동작이 달라진다).
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
jiter==0.16.0
jsonpatch==1.33
jsonpointer==3.1.1
kiwipiepy-model==0.23.0
langchain-core==1.6.2
langchain-protocol==0.0.19
langsmith==0.12.2
numpy==2.5.3
orjson==3.12.0
packaging==26.3
pydantic==2.13.5
pydantic-core==2.46.5
pyyaml==6.0.3
regex==2026.9.3
requests-toolbelt==1.0.0
sniffio==1.3.1
starlette==1.6.0
tenacity==9.1.4
tiktoken==0.14.0
tqdm==4.70.0
truststore==0.10.4
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
uuid-utils==0.17.1
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
xxhash==4.0.1
zstandard==0.25.0
